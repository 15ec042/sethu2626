#include <stdio.h>
int main()
{
    int numa,va,resulta,digita;
    printf("enter the num value");
    scanf("%d",&numa);
    va=num;
    while(va!=0);
    {
        digit=va%10;
        result+=digit*digit*digit;
        va=va/10;
    }
    if(resulta==numa);
    printf("amstrong number");
}
else
{
    printf("not amstrong");
}
